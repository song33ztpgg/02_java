package java;
/**
 
 Map map; 
 map = new ArrayList()
 
 
 

■ Set  
Set set = new HashSet()   
set.add(value)
-중복정의 안됨 
-(value,value,value) 형식

■ List
List list = new ArrayList()
list.add(value) 
-중복정의 가능 
-[value,value,value,]형식

■ Map
Map map = new HashMap();
map.put(key,value)

map.get(key) 
-value


StringTokenizer()



 
 
■ 제정의 클래스 맴버 변수의 출력 내용을 참고하고 온다 
String   -> String() 여기서 정의한 내용을 가져온다 
add      -> add() 여기서 정의한 내용을 가져온다
set      -> set() 여기서 정의한 내용을 가져온다
book     -> toString() 여기서 정의한 내용을 가져온다 
map		 ->key = value 로 제정의 
 * */

import java.util.HashMap;
